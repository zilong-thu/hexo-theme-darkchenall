<%var str = '',amount=40;
if (site.tags.length)
{
  str += '<div class="widget tagcloud"><div class="entry panel-body">';
  str += tagcloud(site.tags.sort('posts.length',-1).limit(amount), _.extend({amount: amount,color: true,min_font: 10,max_font: 30,start_color: '#66FFFF',end_color: '#669900'},options));
  str += '</div></div>';
}%><%- str%>
